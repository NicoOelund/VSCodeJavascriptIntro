// Global variables
let gameArea, scoreEl, timerEl, endMessage, startBtn;
let score = 0;
let timeLeft = 30;
let timerInterval = null;
let gameActive = false;
let circle = null;

// Entry point
// TODO: Add event listener for window load that calls initApp

function initApp() {
    // TODO: Select DOM elements and assign to global variables
    // TODO: Add event listener to start button
}

function randomPosition() {
    // TODO: Calculate and return a random position for the circle inside the game area
}

function createCircle() {
    // TODO: Create a new circle, position it randomly, and add it to the game area
}

function handleCircleClick() {
    // TODO: Handle what happens when the circle is clicked
}

function startGame() {
    // TODO: Start or restart the game, reset score and timer, and show the first circle
}

function endGame() {
    // TODO: End the game, show the final score, and clean up
}
